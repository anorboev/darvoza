# .NET
bin/
obj/
*.user
.vs/

# Secrets / local config — NEVER commit
.env
*.env
appsettings.*.local.json
policy.local.yaml
policy.yaml

# Audit output (runtime artifact)
audit/
*.jsonl

# Cowork↔CC loop config is version-controlled for this repo.
# (Overrides the global ~/.gitignore_global which ignores .claude/ and .claude/**.)
!.claude/
!.claude/**
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Auto-generated session handoff snapshots (regenerated every session; not deliverables).
# Real committed docs (e.g. the A01-T6 writeup) still live elsewhere under docs/.
docs/handoffs/

# OS
.DS_Store
Thumbs.db
