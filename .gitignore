# .NET
bin/
obj/
*.user
.vs/

# Secrets / local config — NEVER commit
.env
*.env
.env.*
!.env.example
appsettings.*.local.json
policy.local.yaml
policy.yaml

# Audit output (runtime artifact). Anchored to the repo root so it does NOT also match the
# src/Darvoza.Gateway/Audit/ source directory under core.ignorecase=true (Windows/macOS) — G-16.
/audit/
*.jsonl

# Local agent/workflow config — machine-specific, not part of the public repo.
.claude/
/CLAUDE.md

# Auto-generated session handoff snapshots (regenerated every session; not deliverables).
# Real committed docs (e.g. the A01-T6 writeup) still live elsewhere under docs/.
docs/handoffs/

# OS
.DS_Store
Thumbs.db
